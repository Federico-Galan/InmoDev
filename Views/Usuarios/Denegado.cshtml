@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="text-center py-5">
    <i class="fa-solid fa-shield-halved fa-4x text-danger mb-3"></i>
    <h1 class="h2 fw-bold text-danger">Acceso Denegado</h1>
    <p class="text-muted lead">No tiene los permisos necesarios para acceder a este recurso.</p>
    <a class="btn btn-primary mt-3" asp-controller="Home" asp-action="Index">Volver al Inicio</a>
</div>
